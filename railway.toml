[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Persistent volume for data storage
[[volumes]]
mountPath = "/app/data"

# This ensures data.json and sessions/ persist across restarts
